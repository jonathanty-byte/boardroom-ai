# BoardRoom AI — Environment Variables
# This app uses BYOK (Bring Your Own Key) via the browser UI.
# No server-side env vars are required for basic usage.

# Optional: set a default OpenRouter API key for development
# NEXT_PUBLIC_OPENROUTER_API_KEY=sk-or-...
